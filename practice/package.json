{
  "name": "my-project",
  "source": "src/pages/login.html",
  "scripts": {
    "start": "parcel src/*.html",
    "build": "rm -rf .parcel-cache && parcel build src/*.html"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "commitlint": "^19.2.2"
  },
  "engines": {
    "node": "18.x"
  }
}
